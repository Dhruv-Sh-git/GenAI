# GenAI
we can combine llm's with tools to do web search or perform any other tasks which cannot be done llm models predominantly to generate a summarised output , we can also attach our llm to databases to store informationn as well as get the required data from stored information to generate a niche desired output .

packages:-
uv(package manager)
uv venv
uv add langchain
uv add ipykernel (for jupyter notebook)
to run streamlit app:
streamlit run .\langchain_basics\qnachatbot.py